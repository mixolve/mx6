# Conventions

- Use English only in repository text and source content. Do not add Cyrillic characters to project files.
- Keep file content in Latin script only for identifiers, comments, strings, docs, and helper text.
- Do not keep commented-out code or commented-out lines in committed files. Remove dead code instead of disabling it with comments.
- Keep comments only when they document required behavior or non-obvious implementation details.
